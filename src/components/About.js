import React from "react";
import Icon from './Icon';
// import styles from "../styles/components/_about.scss";

const About = (props) => {
    return (
        <div>
            I'm a passionate Web Developer interested in computer science, web and software development. My work consists (mostly) of front-end development, although I like to dabble in the back-end ocassionaly :)
        </div>
    );
}

export default About;